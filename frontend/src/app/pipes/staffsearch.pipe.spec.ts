import { StaffsearchPipe } from './staffsearch.pipe';

describe('StaffsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new StaffsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
